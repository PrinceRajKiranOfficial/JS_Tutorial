using HTMl and CSS we can only built static webpages
using JS we can built dynamic webpages

in C programing we can store same data type
in JS we can store different datatype

Before 1995 we have only static web pages.
So,Brandan Eich in 1995 company called Netscape made JS.

So,first name of javascript is mocha
then change it to live script.

/*since java was booming ad JS wasn't not, So Brandan Eich change its name to JavaScript.*/

Other name of JS is TypeScript